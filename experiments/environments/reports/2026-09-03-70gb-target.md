# 70-GiB host migration and larger X-Token workload

Status (2026-09-03 17:35 UTC): **Training r4 terminated with CUDA OOM at
16:02:37 UTC, before the first optimizer update. The full-batch teacher-logit
buffer requested 37.09 GiB per 24-GiB GPU. TensorBoard has resource telemetry
but no train scalars. The corrected CPU-logit transport canary and exact
resolved-config parity passed. That training crossed Ray's 95% RAM protection
and terminated before update 1. A lossless FP32 bit-packing canary passed under
`vdt-xtoken-cpu-logits-r9`; its config passed, but training reached cgroup peak
and failed during teacher offload before update 1. The separate allocator-cache
continuation `vdt-xtoken-cpu-logits-r10` then failed in its canary before model
or training work because both GPUs had entered a driver recovery state. Exact
`nvidia-smi` diagnostics now report `gpu_recovery_action=Reboot` on GPU 0 and
GPU 1. No successful optimizer update is claimed; no further training is safe
until the instance is rebooted and a two-GPU CUDA canary passes.**
Managed r3 previously reached its 90-minute setup timeout, without BrokenPipe.
The later TUNA mirror attempt completed installation in 32.70 seconds using
existing cache, then sequence r3 stopped at data HTTP502. Those attempts are
terminal and must not be replayed. The current continuation is separately
authorized; no dependency reinstall or scientific protocol reduction occurred.
Student verification finished at 11:55:39 UTC: the full
weight-file SHA-256 and all six manifest files matched. The public teacher
completed at 11:44:21 UTC; all three weight shards subsequently matched their
pinned SHA-256 hashes. At that earlier setup snapshot, no training had been invoked.
The previous sequence and student timeout remain immutable failed prerequisite
evidence. SimCT remains failed and was not retried.
Earlier time-stamped setup incidents below are retained, not current success claims.

## Authorized target

- Llama-3.2-1B <- Qwen3-4B; sequence 2048; global batch 64.
- Two RTX 3090 GPUs, microbatch 1, BF16, original NeMo P-KL+CE.
- Ten optimizer updates, as explicitly requested in the latest turn; this
  supersedes the earlier three-update target. Not 5000-step training, H-KL,
  OPD or quality reproduction.
- Full upstream projection preparation, kept separate from the earlier lightweight smoke.
- New user-provided host: driver 580.142, 2 x 24-GiB RTX 3090; initially nearly 70 GiB free disk.
- Container memory limit 114461507584 bytes; `/dev/shm` about 39 GiB.

The old three-update smoke remains historical evidence from a different host and
lighter workload. Its success does not certify the new configuration.

## Source and environment

Branch `vdt/experiments/xtoken`, committed base
`9d5c801ebbaed8c1b7acf7a235876da32a2afb65`, with separately recorded uncommitted
launch/diagnostic changes. No new commit/push is implied.

The new host rebuilds both independent CUDA-12.8 native uv environments, without
relocking or changing the driver. uv is 0.12.7. NeMo source commit
`13a10647ebbf0f940d2b06ea41800b3f2fb46099` is archive-verified: 1876 members,
tree SHA-256 `78f4777b96e9e9131a08302072332c950ef5415e2e054086b962ecda0c58e027`.
The new launcher retains the prior working `NCCL_CUMEM_HOST_ENABLE=0` as an
explicit compatibility choice; this is not a claim that the new host has the
same NCCL bug.

## Transfer incidents, not training failures

1. A local-relayed SSH copy of the already-verified student cache progressed
   at roughly 0.3 MB/s; only its exact transfer processes were stopped.
   Partial data was retained separately, never accepted as a verified model.
2. The first direct Hub helper incorrectly combined `HF_TOKEN` with
   `HF_HUB_DISABLE_IMPLICIT_TOKEN=1`. The CLI therefore reported access denied.
   This **was a helper authentication error, not evidence of missing user approval**.
3. An explicit-auth read-only probe returned account HTTP 200, model config
   HTTP 200, and model-weights HTTP 302 (authorized signed redirect, not followed
   or exposed by the probe). A distinct corrected student-download attempt
   permits the documented `HF_TOKEN` environment authentication path.
4. A direct host-to-host SSH attempt also had poor throughput and was stopped.
   Its ephemeral agent was constrained to the two host keys and a 30-minute
   lifetime, then terminated; no private key was copied to either remote.

No token is saved or printed. The public teacher download is unauthenticated.
Prior failed attempts and partial files are preserved; none count as training.

## Full projection protocol

The checked-in harness mirrors the pinned wrapper's three actual CLI calls:

1. `minimal_projection_via_multitoken`, prep top-32, reverse pass, scale trick,
   special-token mapping; no generator exact-match shortcut.
2. `reapply_exact_map` on the prep artifact.
3. `sort_and_cut_projection_matrix` to runtime top-4, retaining upstream
   scale metadata behavior.

The wrapper does not invoke an embedding model. The old top-4 direct smoke
projection is not treated as equivalent. New manifests record tensor shapes,
index ranges, finite/nonnegative weights and hashes at all three stages.
Reproduction commands and evidence boundaries are in the environments README.

## Verification so far

- Local native/target harness tests: 12 passed.
- After adding sync-only timeout validation: 13 native/target tests passed.
- After adding the approved HTTP-timeout option: 15 native/target tests passed,
  including a mocked execution verifying the exact recorded command, 5400-second
  outer deadline and removal of an inherited HTTP-timeout override.
- Existing X-Token harness tests: 36 passed.
- Git diff whitespace check passed.
- Source provenance verified on the new host.
- Native environment installation, model hashes, GPU canaries, full projection,
  target config gate and actual training remain separate pending gates.

## Installation outcome and pending work

Both independent `uv sync --locked` attempts reached their configured 1800-second
hard limit while downloading dependencies. Their result files report exit 124,
`timed_out=true`, child exit -15, and 120 GPU telemetry samples without telemetry
errors. These are installation timeouts, not CUDA/model/training failures:

| Environment | Evidence directory under `/workspace/vdt-runtime/evidence` | Finished UTC |
|---|---|---|
| X-Token | `20260903T093843.149103Z-xtoken-sync-c33857e4` | 2026-09-03 10:08:43 |
| SimCT | `20260903T094215.802954Z-simct-sync-d2670826` | 2026-09-03 10:12:15 |

No installation retry or time-limit extension was launched. ARS execution rules
require the user to choose an extension/retry. At the subsequent inspection,
both GPUs were idle (0 MiB allocated); disk had about 63 GiB free. The uv cache
held about 5.4 GiB and the HF directory about 2.2 GiB. Cache size is not proof of
a complete installation or model.

The already-started, independently bounded downloads remained running:

- Public Qwen3-4B: evidence `20260903T095736.193493Z-public-teacher-prefetch-59599b91`,
  1800-second limit from 09:57:36 UTC.
- Corrected authenticated Llama download: `direct-student-authenticated-r2/result.json`
  still had `exit_code=null`; its separate 1800-second limit remained in effect.

Do not relaunch either download without inspecting its final state first. No
background training or automatic transition to projection/training is configured.
No old-host workload or transfer remains from this migration.

Exact-value HF-secret audit after the installation outcomes: local artifacts
388 files, remote declared diagnostic/source roots 125 files, zero findings in
both scopes. This is a scoped artifact audit, not an audit of the entire machine,
model/cache binaries, or future output from pending downloads. Small diagnostics
are exported to the local `xtoken-native-2x3090/70gb-evidence` directory.

### Authorized continuation

The user subsequently approved increasing the installation deadline to 90 minutes
and continuing the previously requested test. A new `--sync-timeout-seconds 5400`
option applies only to `run_phase.py ... sync`; canary and training limits remain
unchanged. No lock or dependency version changed. Existing downloads were checked
and remained running, so no duplicate model download was launched.

- X-Token install: `20260903T101842.290948Z-xtoken-sync-ee37d0e7`.
- SimCT install: `20260903T101843.776740Z-simct-sync-33173847`.
- Each invocation is a distinct single attempt with its own 5400-second timer.
- Original 1800-second failures remain retained as historical setup evidence.

### Model-transfer extension and X-Token HTTP failure

The user separately approved 90-minute model transfers. The public teacher's
original 30-minute attempt ended with exit 124 at 10:27:36 UTC. A distinct
5400-second attempt started at 10:30:57 UTC, with diagnostics in
`/workspace/vdt-runtime/model-transfer90-teacher-r2`. Its latest result remains
pending; only one of the three weight shards has a completed cache entry.

The original student wrapper also timed out, but inspection found its download
child still alive. That wrapper's process-cleanup defect is preserved in the
record, not treated as a successful transfer. Rather than launch a duplicate,
`model-transfer90-student-adopt-r3` supervises the exact surviving PID and start
time, with a 5400-second limit on the child's **total** lifetime. This supervisor
does not read credentials. Success requires all six files to match the prior
size/SHA-256 manifest; its latest state is pending. The new transfer helper uses
a process group so future timeout cleanup covers its children.

Completed Hugging Face files are reusable. The installed Hub CLI 1.29.0 uses
process-unique temporary download names, so unfinished file sizes must not be
presented as reusable or verified model weights.

The authorized X-Token installation continuation ended at 10:40:02 UTC with
exit 1 after 1280.692 seconds, **not** its 5400-second outer timeout. Its log says
`torch==2.11.0+cu128` failed to download/extract because uv's internal
`UV_HTTP_TIMEOUT` remained 30 seconds. This is a network/download failure, not
evidence of a broken lock, CUDA error, OOM or training failure. About 59 GiB of
disk remained free. The SimCT installation remains active with its original
90-minute deadline.

After this failure, the user explicitly approved one retry with
`UV_HTTP_TIMEOUT=300`, retaining the same lockfile and 5400-second outer bound.
It started exactly once at 10:51:05 UTC under evidence directory
`20260903T105105.357336Z-xtoken-sync-63f168d2`; `command.json` records
`env UV_HTTP_TIMEOUT=300 uv sync --locked` and the 5400-second deadline.
The launcher rejects nonpositive values and HTTP overrides for non-sync phases.
No model transfer, active SimCT installation, CUDA setting or training parameter
was restarted/modified by this action. GPU canaries, full projection, config
validation and the three-update training test are still pending.

## Material Passport

- Origin Skill: academic-research-suite / experiment-agent
- Origin Mode: run
- Origin Date: 2026-09-03
- Verification Status: UNVERIFIED (target training)
- Version Label: target_setup_v7_autonomous_data_recovery
- Materials: exact pinned NeMo source/default YAML/projection wrapper, local
  launchers, existing verified student file manifest, protected access probe,
  current-host hardware and installation logs.
- Evidence: static source inspection plus live setup/access checks.
- Not established: target optimizer completion, sustained throughput, model
  quality, full-paper reproduction, OPD or SimCT training.

## Latest ten-update request and guarded execution

At 11:19:07 UTC, one foreground sequence was started under
`/workspace/vdt-runtime/sequence-target-4b-2048-b64-full-10steps-r1`.
Its `result.json` records `waiting_for_existing_setup`, no invoked training
phase and no completed phases. It waits for the exact existing X-Token sync,
teacher transfer and adopted-student integrity result; it does not relaunch any
of them. Readiness ends at 12:22:05 UTC, just after the latest existing setup
deadline. Each prerequisite retains its own original hard deadline.

On verified readiness, the sequence executes dependency check, CUDA canary,
model hashes, data, full projection, config validation and training exactly once.
Only the five previously documented CUDA-13 optional dependency exclusions may
pass the native metadata check; any other incompatibility blocks execution.
GPU occupancy and cgroup-memory headroom are checked before expensive phases.
Launcher, lock and NeMo source hashes are frozen before waiting. Any failed
phase stops the sequence without automatic retry. Training process exit zero
would still require an independent finite-metric and ten-update review.

The model pair, sequence 2048, batch 64 and upstream optimizer/scheduler remain
unchanged. Only the execution stop changes from three to ten updates; warmup
remains 250 and cosine horizon remains 4750, not compressed into ten steps.
The deterministic contiguous dataset prefix now targets 704 character packs
and must yield at least 640 full-length tokenized samples. No samples are
invented or repeated to satisfy the count. Data is still a bounded prefix,
not the complete paper training corpus.

Local verification: 21 native/target/sequence tests and 36 X-Token harness
tests passed (57 total), plus `git diff --check` and remote Python syntax check.
An initial existing cleanup test hit a `/proc` reaper race; a narrow test-only
FileNotFoundError/ProcessLookupError guard fixed that fixture, with runtime
cleanup behavior unchanged. Config validation now uses the pinned Pydantic
`TeacherConfig.policy_config()` API instead of indexing it as a dict. This
fix is only in the validator, not NeMo's model/loss/training implementation.
The four updated remote launch/preparation scripts match local SHA-256 hashes.

Student download temporarily stopped increasing after 10:41 UTC, but a new
11:17 UTC write brought it to 1,944,824,151 of 2,471,645,608 bytes. Therefore it
is not classified as a proven dead process; its approved total 90-minute
deadline still applies. Neither partial bytes nor a running PID prove a valid
model. No optimizer update or target throughput has yet been observed.

### Independent SimCT installation failure at 11:24 UTC

The 90-minute SimCT sync ended early after 3933.508663 seconds with exit 1,
`timed_out=false`, and 262 valid GPU telemetry samples. Its `torch==2.9.1+cu128`
download failed during extraction with uv's internal network timeout still at
30 seconds. This is not the outer 5400-second deadline, a CUDA error, or evidence
that SimCT's lock is invalid. Evidence is
`evidence/20260903T101843.776740Z-simct-sync-33173847/{result.json,stdout.log}`.
No retry was launched. SimCT is independent and is not an input dependency of
the already-running X-Token ten-update sequence.

### Protected read-only Range diagnostic

At 11:27:51--11:27:55 UTC a single protected request tested one byte at the
student partial-file offset 1,944,824,151. The authorized Hub HEAD returned 302;
the signed CDN GET returned 206 with the exact expected Content-Range for a
2,471,645,608-byte file. Exactly one byte was read and no model bytes were
written. The HF bearer token was not forwarded to the CDN, persisted or printed;
no signed URL was emitted. Result: `student-range-diagnostic-r1/result.json`.

This establishes Range support, not the correctness of the existing incomplete
prefix. Any future resumed file must still pass the full pinned SHA-256 and all
six student-manifest checks. A possible one-time 30-minute Range continuation
was requested from the user, conditional on the existing transfer timing out;
it has not been authorized or launched at this snapshot.

## Terminal sequence outcome

At 11:31:32 UTC the adopted student process reached its approved 5400-second
total-lifetime limit (checked on a 30-second cadence). The supervisor stopped
that exact PID and confirmed `process_state=null`, `status=timeout`, and
`transfer_verified=false`. The preserved partial file is 1,944,824,151 bytes,
leaving 526,821,457 bytes of the expected file; no full-model hash was claimed.

At 11:31:37 UTC the sequence recorded exit 1, `status=stopped`,
`training_phase_invoked=false`, `phases=[]`, dependencies
`[waiting, waiting, failed]`. **This is a setup/transfer failure before any
target CUDA canary, projection, config execution or optimizer update**, not a
failed loss/gradient run. Both GPUs remained idle with zero allocated MiB;
51 GiB disk was free and all cgroup OOM event counters were zero.

The sequence is terminal and must not be replayed or have its result rewritten.
The independently authorized X-Token installation and teacher transfer were
not cancelled and were still active at the final check. Their successful
completion alone will not restart the stopped sequence. A new transfer attempt
and continuation need explicit approval, immutable new attempt evidence, full
model verification, and the unchanged ten-update protocol. The proposed Range
continuation remains unstarted while awaiting the user's reply.

Local code changes remain uncommitted; no push, environment relock, scientific
config reduction, remote deletion or infrastructure change occurred this turn.

Post-outcome exact-value HF-secret audit: 422 local files and 145 files in the
declared remote source/diagnostic roots, zero findings in both scopes. No secret
file was created for the Range probe; its credential existed only in memory
and encrypted SSH stdin. Small terminal results, source fingerprints and the
Range diagnostic were exported locally; no heavy model/checkpoint was copied.

## User-approved HTTP resume and new ten-update continuation

The user explicitly approved the proposed one-time student resume, bounded to
30 minutes, followed by the unchanged ten-update workload when all gates pass.
`resume_student_transfer.py` started once at 11:41:45 UTC, with result under
`student-http-resume-r4/result.json`. It copies the inactive 1,944,824,151-byte
partial into a private, separate staging directory, preserving the original;
the remaining request is exactly 526,821,457 bytes. The full file must match
SHA-256 `68a2e4be76fa709455a60272fba8e512c02d81c46e6c671cc9449e374fd6809a`,
then all six student manifest files are rechecked before `transfer_verified=true`.
Publication uses a no-overwrite hard link; no incomplete file is used for training.
The HF token is relayed only via encrypted SSH stdin, never persisted, logged,
or forwarded to the signed CDN. No automatic network retry is configured.

One new sequence was started at 11:44:31 UTC:
`sequence-target-4b-2048-b64-full-10steps-r2`. Its declared predecessor is the
terminal setup-only r1. The guard requires zero predecessor execution phases,
no training invocation and matching scientific launcher/data/config/lock hashes.
Its new student dependency is `student-http-resume-r4/result.json`; the existing
X-Token sync and teacher result are reused without rerunning them. Readiness
still ends at 12:22:05 UTC, not an extension of either producer's timer. The
future training artifact directory is `target-4b-2048-b64-full-10steps-r2`.

The current turn passed 27 native/target/resume tests plus 36 X-Token harness
tests (63 total), diff whitespace check, and remote Python syntax checks. The
remote resume helper SHA is `e54beae226517068f51f69cd221c216fae60172f3f5a4f7b6b822d1c410f052b`;
the continuation launcher SHA is `64528b31b20f1ad3ac84f49c7ffd15861b8ea20c6d1f5268bd463e73b857ccfe`.
These are setup tests and provenance, not proof that ten training updates fit or complete.

The already-running teacher download finished with exit 0 after 4403.744291
seconds at 11:44:21 UTC. A separate read-only hash check then matched all pinned
shard hashes: `328a91d3...223`, `6cd087b3...ca5`, and `e4bf4369...ca1` (full
values remain in `prepare_xtoken_target.py`). Full model/config/projection gates
will still run before training.

### Verified student resume outcome

The one authorized resume finished at 11:55:39.028387 UTC with exit 0 after
833.503531 seconds (about 13 minutes 54 seconds). It received the exact
526,821,457-byte suffix and verified the complete 2,471,645,608-byte weight file
against SHA-256
`68a2e4be76fa709455a60272fba8e512c02d81c46e6c671cc9449e374fd6809a`.
All six student files passed the pinned manifest. The terminal result is
`status=verified`, `transfer_verified=true`, with the original partial preserved.
The `remaining_bytes` field records the initial suffix request; it equals
`received_bytes` at completion and does not mean bytes are still missing.
No credential file was created; no model/checkpoint was downloaded to local disk.

At 11:56:01 UTC the new sequence recorded dependencies
`[waiting, ready, ready]`: X-Token installation pending, teacher ready, student
ready. It had `training_phase_invoked=false` and `phases=[]`; its
`optimizer_updates=10` field is the requested target, not an achieved count.
The existing installation retains its 5400-second deadline from 10:51:05 UTC.
The one-shot continuation is active, but no recurring monitor or completion
notification was created. Actual training and finite ten-update metrics remain
unverified. No additional install, transfer, or sequence retry was launched.

## Diagnosed output-pipe failure and authorized managed recovery

At 12:05:23 UTC the X-Token installer wrapper stopped after 4458.302605 seconds,
exit 127, `error_type=BrokenPipeError`, `timed_out=false`. It had 297 valid
telemetry samples. The uv log still showed downloads, not a completed install.
The launcher printed progress into an external pipe; EPIPE propagated into its
OSError handler and finally block, which terminated the child process group.
The exact reason the external reader disconnected is not established by these
logs. This is an orchestration failure, not evidence of CUDA/OOM or a broken
model. At 12:05:31 UTC r2 stopped with zero phases and no training invocation.

The user authorized fixing the launcher and one cache-based continuation.
`BestEffortOutput` now isolates EPIPE from durable logging while retaining
child failure/timeout behavior; `Tee` writes the durable log before the optional
console. A real pipe-reader-disconnection test checks both successful child
completion and absence of Python's shutdown exit-120 issue. The managed worker
uses Supervisor, disables automatic restart/start, claims an exclusive attempt
directory and serializing lock, strips inherited credentials, and preserves
the original 5400-second install / HTTP-300 bounds. No lock/model/scientific
parameter changed. Local tests: 39 X-Token plus 31 environment tests passed.
Deployment/start and post-SSH-disconnection verification are separate gates;
no optimizer update or performance result is implied by this code verification.

### Managed launch verified at 13:30 UTC

Preflight passed on the remote host: exact setup-only r2, failed installer
BrokenPipe result, successful verified model results, unchanged scientific
scripts and native lock, and two idle GPUs. Four deployed launcher/wrapper
files matched their local hashes. The previous two replaced scripts were
preserved in `launcher-backup-before-managed-r3/` outside the worktree.

The dedicated Supervisor program `vdt-xtoken-10steps-r3` was registered STOPPED
with `autostart=false`, `autorestart=false`, `startretries=0`, then explicitly
started exactly once at 13:29:25.570600 UTC. Worker PID 6685 has parent PID 618
(Supervisor), not the initiating SSH session. That SSH command exited normally.
A new connection 50 seconds later found the same worker RUNNING and install
progress advancing at 0, 15, 30 and 45 seconds, confirming transport-independent
execution. No portal/network endpoint or unrelated service was changed.

- Managed status: `managed-target-10steps-r3/result.json`.
- Install evidence: `evidence/20260903T132925.639367Z-xtoken-sync-c7e43d32/`.
- Install child PID: 6691; command `env UV_HTTP_TIMEOUT=300 uv sync --locked`.
- Install deadline: 5400 seconds from 13:29:25 UTC (about 14:59:25 UTC).
- Requested continuation: `sequence-target-4b-2048-b64-full-10steps-r3` only
  after successful installation. It has not started at this snapshot.
- Student/teacher weights are reused; no new model transfer, SimCT retry,
  dependency relock, upstream algorithm change, commit or push was performed.

The new managed execution is persistent across the observing SSH disconnect,
not a recurring Codex automation. Supervisor will not replay it after failure,
completion or reboot. Actual ten-update metrics still require terminal review.

## TUNA transport fallback: installation completed

Managed r3 ended at 14:59:25.656038 UTC, exit 124, `timed_out=true`,
`console_disconnected=false`, after 5400.015907 seconds. Its worker subsequently
recorded `setup_failed`, no training invocation. Unlike the earlier BrokenPipe
failure, this was the approved outer download deadline. No sequence r3 existed.
The log showed Torch and most CUDA libraries already downloaded; cached data
and all prior failed results were retained. No running workload needed killing.

The user explicitly authorized the proposed Tsinghua/TUNA mirror continuation.
The new helper creates a private transport-only installation view, changing
only cuBLAS/cuDNN wheel download URLs. Canonical `pyproject.toml`, `uv.lock`,
`.python-version`, dependency graph, package versions/hashes, pinned source and
the scientific workload remain unchanged. Torch keeps its official CUDA12.8
source and was reused from cache. TUNA filenames and advertised SHA-256 are
checked before installation; uv checks full artifact hashes during sync.

- Canonical native lock SHA-256:
  `145d512cf6e56deec88eacfde4159ba97fd55496a26e26d5aec8d33b7ba357cb`.
- Transport-view lock SHA-256:
  `a103f363618532025c281185b255b7af8a66b3ea6c84a4adf59482ccdd5a6183`.
- Reversing exactly the two URL substitutions reconstructs the original lock
  byte-for-byte; parsed graph equality is also checked.
- No global index setting, TLS bypass, re-resolution, cache removal, model
  transfer, SimCT retry, upstream code change, commit or push occurred.

Supervisor program `vdt-xtoken-10steps-mirror-r4` started exactly once at
15:30:56.302719 UTC, PID 7790, parent Supervisor PID 618, after exact predecessor,
scientific-hash, idle-GPU, syntax and mirror preflight checks. It survived the
initiating SSH connection closing. Automatic restart/start are disabled and the
exclusive managed attempt directory prevents replay. The old replaced launchers
are preserved in `launcher-backup-before-mirror-r4/`.

Installation evidence:
`evidence/20260903T153056.375312Z-xtoken-sync-199631cf/`.
The actual command uses `uv sync --frozen --project <private-view>` with the
existing cache and dedicated venv. It prepared exactly two packages in 30.74 s,
then installed 244 packages in 973 ms. The outer runner finished at
15:31:29.075031 UTC, exit 0, elapsed 32.699092 s, no timeout/disconnection and
three valid GPU telemetry samples. This short completion uses substantial
previously downloaded cache; it is not a 32-second cold environment rebuild.
Transport proof/results live under `mirror-installs/20260903T153057Z-527d5363/`.

Only then did the previously unused sequence r3 start at 15:31:29.091069 UTC,
continuing the setup-only sequence r2. Its dependency check returned exactly the
five documented optional-CUDA13 missing requirements, not a clean metadata
check. The dual-GPU CUDA canary exited 0 under
`evidence/20260903T153129.563256Z-xtoken-canary-df93c5e4/`.
At the 15:32 UTC snapshot, cached-model verification was active, no optimizer
update had been run, about 40 GiB disk was free and all cgroup OOM counters were
zero. Model/data/full-projection/config/training gates remain distinct.

Verification for this change: 41 environment/target/mirror tests and 39 X-Token
harness tests passed (80 total), `git diff --check`, remote Python/shell syntax,
and matching remote/local hashes for all three deployed Python files. The tests
cover exact URL-only normalization, rejected wrong hashes/hosts, absence of a
mirror wheel, preserved child exit codes, no retry, exclusive attempt identity,
and rejection of any predecessor that already invoked training or sequence r3.

### Terminal data-source failure after successful install

Sequence r3 stopped at 15:32:24.712126 UTC, exit 1, during `data`.
The managed worker recorded the same terminal outcome and Supervisor is EXITED;
there is no active training process. The following gates had passed:

- Dependency check: exactly the five documented exclusions, explicitly recorded
  as nonzero metadata with accepted scope; not clean metadata health.
- CUDA canary: both ranks report Torch `2.11.0+cu128`, CUDA `12.8`, RTX 3090,
  NCCL sum `3.0` and `finite_backward=true`; terminal exit 0. Upstream optional
  kernel/docstring warnings are preserved, not silently removed.
- Models: student manifest and all teacher shard hashes verified; exit 0 in
  `evidence/20260903T153204.490413Z-xtoken-target-models-1-86638be7/`.

The data fetch successfully preserved offsets 0 and 100 (200 contiguous rows,
34 complete character packs). At the next page request, offset 200, Dataset
Viewer returned `HTTP Error 502: Bad Gateway` from `urllib.request.urlopen`.
Evidence: `evidence/20260903T153213.232160Z-xtoken-target-data-1-df58f4cd/`;
exit 1 after 11.477188 s, not timeout or BrokenPipe. No dataset revision/hash
assertion failed on the first two pages. No complete dataset manifest/Parquet,
full projection, config-validation result or optimizer update was produced.

This is an external HTTP failure in data acquisition, not a CUDA/VRAM, model,
package mirror or training-math failure. Whether the endpoint has recovered is
not established by the terminal log. The original pages, attempt markers and
failure remain intact. Per academic-research-suite's no-auto-retry rule, further
data retry/resume and a fresh continuation need user authorization; no dependency
reinstall, scientific reduction or silent replay is needed or permitted.

Post-terminal scoped exact-value HF-secret audit returned zero findings in both
the local project/artifact roots and declared remote source/evidence/transport/
sequence/data roots. Small installation, canary, model-verification, data-failure
logs and manifests were exported to `70gb-evidence/mirror-r4/`; no weights or
checkpoints were copied locally. No credential file was created for this work.
Changes remained uncommitted/unpushed; at that snapshot the completed environment
stayed available, with neither training nor an automatic retry active.

### Authorized autonomous data continuation (15:50 UTC snapshot)

The user subsequently requested automatic deployment/debugging until success.
Operational retries are now authorized within the existing ten-update contract;
this does not authorize model/data/loss/shape changes, new rented infrastructure,
deletion, commit or push. Historical failure records remain unchanged.

Added `resumable_target_data.py` and `continue_target_data.py` as separate
operational helpers. No previous scientific script or canonical lock was edited.
The data helper bounds transient HTTP/transport retries to eight per page and a
1500-second deadline, records attempts/backoff, and validates revision, contiguous
row indices, untruncated text and cache hashes. Original r3 pages remain intact;
without revision sidecars they are not silently reused.

After 47 environment tests plus 39 harness tests passed (86 total), remote
preflight rehashed the frozen source and every cached model file, checked the
successful install evidence and two idle GPUs. Supervisor program
`vdt-xtoken-data-continuation-r5` started exactly once at
2026-09-03T15:48:40.069545Z, PID 8283, parent 618. Its result is
`sequence-target-4b-2048-b64-full-10steps-r4/result.json`; target artifacts use
`target-4b-2048-b64-full-10steps-r4/`. No installer/model transfer was rerun.

The new canary exited 0; dependency metadata retains exactly the known five
optional exclusions. Data fetch has progressed past the previous offset-200
failure to 1800 contiguous rows / 212 character packs at 15:49:46.894197Z.
This is setup progress, not an optimizer update. Full projection, resolved
configuration and ten actual finite updates still require verification.

A 15-minute thread heartbeat `xtoken-2x3090-ten-updates-until-verified` continues
read-only monitoring while active and evidence-first bounded operational
recovery after diagnosed failures. It must not duplicate an active workload,
edit frozen source mid-run, or call requested-step metadata a success. It pauses
only after terminal metric/lineage review or requires user direction for a
materially different scientific/resource choice. Notifications are limited to
meaningful changes, completion or needed decisions.

At 15:56:58 UTC, data preparation completed successfully with 11300 rows and
707 character packs. Evidence directory:
`evidence/20260903T154904.897101Z-xtoken-data-recovery-r4-48924a61/`.
The pinned-revision/hash manifest and Parquet are complete; full-token-length
capacity is still separately checked by the later configuration gate. Recorded
429 backoffs recovered without changing identity or dropping any prefix rows.
At 15:56:58.848137Z the sequence automatically entered the original full
projection pipeline, with its resource gate passed. No training update is
claimed from this setup transition.

All four projection commands/audits exited 0, including the original dense
projection's compatibility test, exact remap, top-4 cut and artifact audit.
The config/data-length gate exited 0 in
`evidence/20260903T155937.060528Z-xtoken-target-config-1-4aa0822d/`.
At 15:59:55.631117Z the worker automatically invoked training exactly once,
without any model/shape/loss/lock change. Training startup is not evidence of
completed optimizer updates or finite terminal metrics.

The configuration validator observed 708 dataset samples, including 707 samples
of full length 2048 under each tokenizer (one final partial sample). Both
tokenizers therefore exceed the required 640 full-length-sample capacity gate;
the original data loader/shuffle is unchanged. At the 16:02 UTC training
snapshot the log entered `Step 1/10` and both GPUs were active, using about
9874 MiB each. This header precedes completion of an optimizer update. Actual
loss/step counters and terminal metrics are still pending. Training evidence:
`evidence/20260903T155955.643101Z-xtoken-target-train-1-a85783bb/`.

### Full-batch CUDA buffer diagnosis and bounded CPU-transport retry

The above training terminated at 16:02:37.707846 UTC, exit 1, elapsed 162.064 s.
The error originates in `ensure_teacher_ipc_buffer`, allocating
`[32, 1, 2048, 151936]` FP32 logits per rank: 37.09375 GiB per GPU, or
74.1875 GiB across both ranks. It is not an installer, network, or BrokenPipe
failure. `Step 1/10` is a pre-update header; independent TensorBoard extraction
finds only resource scalars, no train scalars (0 completed updates).

An explicitly recorded, private NeMo overlay moves only teacher-logit storage
to same-node, cgroup-accounted RAM using memfd shared mappings. The consumer
copies only its selected microbatch back to CUDA. Full vocabulary, FP32 logit
values, BF16 model configuration, loss, optimizer, sequence, global batch and
microbatch are unchanged. Original NeMo is preserved; this is a modified
operational transport, not unmodified upstream execution. The two upstream
files and exact anchors are pinned before the separate overlay is built.

First canary (`cpu-r5`, Supervisor r6) failed before training because the
standalone CPython build omits `fcntl.F_ADD_SEALS`. This failed canary is retained
in `evidence/20260903T163447.581724Z-xtoken-cpu-r5-canary-eb98d959`.
The corrected helper uses the host-verified Linux UAPI constants and reads back
the size seals; it does not remove sealing or alter OS limits. Original helper
bytes are backed up under `launcher-backup-before-cpu-r6` and the first overlay
is immutable. Supervisor `vdt-xtoken-cpu-logits-r7` started once (PID 14786).

The new sequence rehashes weights, Parquet, projection and prior sources,
requires two idle GPUs and at least 90 GiB reclaimable cgroup headroom, and
reuses data/projection read-only. No download, install, projection recomputation,
or optimizer-state resume occurs. Canary/config deadlines are 300 seconds;
the single training invocation remains bounded to 1800 seconds. Canary checks
bitwise cross-process transport, downstream test loss and gradient, not full
X-Token optimizer equivalence. Config parity allows only run/artifact paths.
Local tests: 53 environment plus 39 harness tests pass (92 total).

The second canary (`cpu-r6`) reached the CUDA comparison but failed because
cross-GPU peer access is unsupported on this host. CPU sharing itself passed
its initial checks. The corrected canary uses two processes on the same GPU
for each round, testing GPU 0 and then GPU 1. This matches the teacher/student
same-GPU IPC baseline and does not require P2P. No system or driver changes.
`cpu-r5` and `cpu-r6` are terminal, with training never invoked.

The third canary (`cpu-r7`) exited 0 with four rank/GPU confirmations: bitwise
logit values, downstream test loss, and gradients. Evidence:
`evidence/20260903T164054.438723Z-xtoken-cpu-r7-canary-0a1b3b80`.
Config validation exited 0 in
`evidence/20260903T164109.637663Z-xtoken-cpu-r7-config-06680acd`.
Resolved configuration matches r4 exactly after replacing only target paths;
the data/projection symlinks point to the old rehashed artifacts. The private
overlay is `NeMo-RL-cpu-logits-r3`; original upstream sources remain unchanged.
Before training, reclaimable cgroup headroom was 104002285568 bytes (~96.86GiB).
Supervisor r8 (PID14902) and sequence cpu-r7 are terminal; they must not be replayed.

That cpu-r7 training terminated after 247.285 s in `offload_after_refit`.
Ray reported 105.69/106.60 GiB used and killed workers when cgroup usage
108972797952 bytes exceeded its 95% threshold108738428928 bytes. The kernel
cgroup reported no OOM/oom_kill. The Ray threshold and cgroup remain unchanged.
No optimizer update or train scalar was produced.

The next operational overlay stores the upper 16 bits of each logical FP32
teacher logit only when all lower 16 bits are already zero and values are
finite. Any non-encodable value fails before training; there is no rounding,
fallback, vocabulary reduction or loss dtype change. Consumer slices rebuild
the exact FP32 bit patterns. This is valid for the observed BF16-origin logits
converted to FP32 upstream, and cuts aggregate buffer storage from ~74.19 to
~37.09 GiB. It is a lossless wire/storage encoding, not BF16 loss math.

`cpu-r8` canary evidence
`evidence/20260903T165040.114679Z-xtoken-cpu-r8-canary-6ff01b3c`
has four rank/GPU bitwise value/loss/gradient confirmations plus special-value
round-trip and a deliberate 0.1 rejection. Supervisor r9 (PID21014), sequence
cpu-r8 and overlay `NeMo-RL-cpu-logits-r4` are unique. Training remains gated
on resolved-config equivalence and, if invoked, still has one 1800-second
attempt. This run is not yet a success result.

Cpu-r8 training then terminated after 222.493 s, before update 1, in
`offload_after_refit`: the worker's post-offload allocator wakeup returned
`CUDA error: unknown error`. GPU telemetry explains the location: both GPUs
rose from about 5.25 GiB to 23.67 GiB while 32 full-vocabulary temporaries were
successively freed into PyTorch's CUDA caching allocator. Cgroup memory reached
its exact 114461507584-byte limit, although Linux recorded no OOM kill. This is
not a non-encodable-logit failure; the lossless canary and config had passed.

The distinct cpu-r9 continuation adds `gc.collect()` and
`torch.cuda.empty_cache()` immediately before moving the teacher model to CPU.
This releases already-dead full-vocabulary CUDA cache blocks before D2H model
offload and the allocator wakeup; it does not change live tensors, forward/loss,
optimizer, batch, sequence or system/Ray/cgroup limits. The new overlay pins the
exact upstream worker SHA and records it as the third modified transport/runtime
file. Supervisor r10 (PID26989), target cpu-r9 and overlay r5 were started once.
The preceding attempts remain immutable and must not be replayed. Local checks
now pass 54 environment plus 39 harness tests (93 total).

The r10 canary stopped after 11.875 s in
`evidence/20260903T171232.690559Z-xtoken-cpu-r9-canary-66f39ea6`.
Both ranks failed at CUDA initialization with `CUDA unknown error`, before
transport assertions, configuration validation, model loading, or training.
There were no compute processes and both devices showed zero allocated memory,
but NVIDIA's recovery query returned `Reboot` for both GPUs. A later direct
runtime probe still could not initialize CUDA. This is a host/driver recovery
blocker inherited from the prior CUDA failure, not evidence against the new
cache-eviction patch. Replaying the scientific workload in this state would be
a blind retry, so the next continuation is gated on an instance reboot and a
fresh two-GPU CUDA canary. The current authorization does not include rebooting,
stopping, or destroying the instance.
